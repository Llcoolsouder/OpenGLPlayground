language: cpp
sudo: required
dist: bionic
compiler:
        - gcc
os: linux

before_install:
        - sudo apt-get install -y libglew libglew-dev 
        - sudo apt-get install -y libglm libglm-dev
        - sudo apt-get install -y libglfw3 libglfw3-dev
